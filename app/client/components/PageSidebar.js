import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.nav`
  background-color: ${p => p.theme.colors.iron};
  padding: 1rem;
`

const PageSidebar = () => <Wrapper>PageSidebar</Wrapper>

export default PageSidebar
